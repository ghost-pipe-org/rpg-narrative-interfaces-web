export interface postSessionData {
    title: string;
    description: string;
    requirements: string;
    system: string;
    possibleDates: Date[];
    period: string;
    minPlayers: number;
    maxPlayers: number;
}